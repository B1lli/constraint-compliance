---
name: constraint-compliance
description: >
  Use when the request hits a rule the user taught before — one recorded in a project instruction file,
  a memory note, or a recalled learning item — and this delivery has to follow it: turn that rule into
  acceptance criteria and audit the delivery against them before calling it done.
  当用户这次要做的事落在他以前教过你的一条规矩里（那条规矩记在项目规矩文件、记忆或已召回的学习项中），
  而本次交付必须按它做时使用。
  Not for single-turn answers, work with nothing produced, or a task a redo fixes.
license: Apache-2.0
metadata:
  author: Guangyao Chi (B1lli)
  origin: built-in skill of a production AI work assistant, open-sourced 2026-09
  homepage: https://github.com/B1lli/constraint-compliance
---

# 强遵循门控

这份 skill 只做一件事：当你判断「这次交付不能违反某条规矩，违反了代价很高」时，把规矩变成一份能逐条核对的验收标准，
给本次任务立一个目标，并在收尾时按标准审计自己。
目标立起来之后，写完不算完：目标没审计通过，你就还没做完，继续朝目标工作，不要在这一轮结束前交付。
标完成时会有一次确定性核对（脚本）和一位独立评估者逐条给建议。建议只是建议，不拦你交付，但你要回应它。

整套方法都在你手上：判断值不值、写意图、选档、写标准、立目标、审计，全部在当前上下文里完成。
立目标和标完成不需要专用工具：目标写进工作区的 `.gate/goal.json`，审计用本 skill 自带的 `scripts/gate_check.py`。
下文说的「学习项」泛指你所在环境里持久化下来的已学规矩：记忆文件里的一条、`CLAUDE.md` / `AGENTS.md` 里的一条，
或宿主自带学习机制召回的一条。

## 一、何时用 / 何时不用

你可能是从已学规则块、项目规矩文件或系统提示里那句指路来的，或因为本次请求命中了用户教过的规矩：
那意味着本轮有规矩在管。接着回答下面三问，任一答「不」即退出，本轮零成本。

先用三问判断值不值。任何一问答「不」，就到此为止，本轮零成本。

**一问：这次有没有一条不该被违反的规矩？** 四类情境，命中任一即算有：

1. 用户说「这个交付很重要」「这份要给客户 / 老板看」之类。
2. 用户表达不满或抱怨，比如「我上次教过你，你怎么还犯」——这条规矩已经复发过，普通提醒不够。
3. 一条已召回、你正在按它做事的学习项，本次交付若违反它代价很高：返工、对外失信、违规、损失金钱。
4. 用户没教过，但你识别出本次交付要满足的外部要求：招标条款、模板、规范、法规、格式标准。这类要你自己去查清楚。

**二问：这次做错了，谁会为此付出什么？** 答「我改一下就好」，到此为止。
答案里有客户、合规、钱，或者用户已经抱怨过，往下走。越强越贵：L3 多一次独立评估和若干返工轮，L4 多一个独立验收会话。

**三问：有没有可核对的产物？** 没有文件、没有正式回复、没有要交出去的东西，就没有能审计的对象，到此为止；
单轮问答、闲聊、解释概念同理。规矩只有一两条、你一眼能核对的，写进本轮回复末尾的自检里就够了（L2，见第三节）。

三问都过再往下读。这里没有关键词规则：用不用是你在完整上下文里的判断，不是看到某个词就触发。

## 二、意图洞察

动手前，先在当前上下文里把下面五行写清楚。不调用任何工具，不向用户要输入：材料都在对话、附件和已召回的规矩里。

- **交付物**：最终交出去的是什么，以什么形式，放在哪里。
- **消费者**：谁拿去用，拿去做什么决定。
- **成功信号**：对方拿到后怎么判断「这次做对了」。
- **非目标**：这次明确不做什么，免得目标被自己扩大或缩小。
- **错一次的代价**：内部返工 / 对外交付失信 / 合规风险 / 资金损失，选最重的那一档写。

写不出来的项写「未知」，不要编。代价未知时按「内部返工」算，除非用户已经说了重要或抱怨过。
这五行就是后面目标文件里的 `intent`，评估者会拿它理解你在做什么。

示例（RFP 响应）：
交付物：对客户 RFP 条款 R-17 到 R-21 的逐条响应文档，写进工作区。消费者：销售交给客户采购部，用于评标。
成功信号：每条有独立状态、有证据，无法满足项被显式列出。非目标：不改产品能力，不写整体方案书。
错一次的代价：对外交付，多承诺一条能力就是合同风险。

## 三、选档

| 档 | 什么时候选 | 做什么 | 成本 |
|---|---|---|---|
| L2 | 违反代价可承受、能重做；或规矩一眼能核 | 本轮回复末尾附一份自检清单，逐条打勾；**不立目标** | ≈0 |
| L3 | 值得立目标；做错了要返工，但影响在内部 | 立目标（level L3）；标完成前跑确定性核对，建议做独立评估 | 一次评估 + 返工轮 |
| L4 | 对外交付、合规、资金，或这条规矩已经复发过 | 立目标（level L4）；标完成前跑确定性核对，且必须做独立评估 | 一个独立会话 |

复发升档：用户抱怨「上次教过还犯」的规矩，至少 L3；若同时是对外交付，L4。
拿不准 L3 还是 L4：默认 L3。多花一个独立会话，只在代价明确落在对外 / 合规 / 资金时才值。

**要不要精确化验收标准（`precision`）：**

- `precise`：按第四节的规则逐条写。更准、更可复现，评估者能逐条核；代价是你要多写、评估者多核。
  交付物有结构（条款、章节、多个文件），或规矩带具体专名 / 阈值时选它。
- `coarse`：直接把学习项自带的审核题目原样当标准，一条学习项一条标准。规矩短、任务小、时间紧时选它。
  没有学习项（第 4 类情境）时不能选 `coarse`：没有现成题目可抄。

## 四、验收标准（rubric）怎么写

每条标准是一句关于产物的自然语言断言，配四样东西：

- `howToJudge`：拿着产物怎么判过没过。具体到「打开哪里、看什么、什么算过」。
- `expectedEvidence`：证据类型。`artifact`（文件产物）/ `reply`（回复正文）/ `tool_record`（工具调用记录）/ `any`。
- `hard`：硬标准（不满足不能交付）还是软标准（建议）。
- `sourceItemId`：来自哪条学习项；来自用户本次原话或外部要求时不填。

硬规则：

1. **专名必须有字面来源。** 章节名、文件名、路径、阈值、日期、编号，必须能在用户原话、学习项正文或本次任务材料里找到原文。
   找不到就把这条标 `hard: false`，并在 `text` 里写明这是你的假设。
2. **禁止空话。** 「符合要求」「质量合格」「专业」「完整」「逻辑清晰」不能单独成一条。
   一句话测试：换一个人拿着产物，能不能按 `howToJudge` 得出同一个结论？不能就重写。
3. **每条都要能从产物判出来。** 「用户会满意」「客户能接受」判不出来，删掉或改成能看见的东西。
4. **条数不超过 12。** 超了就按交付物的结构合并，或只留代价最高的那些；其余放进你自己的自检清单。
5. **多条学习项合并成一份。** 一个会话一个目标，所有规矩进同一份 rubric，每条记来源。
6. **用户抱怨过的那条放最前，标硬。**
7. `id` 用 c1、c2 顺序编号。

**完整示例（RFP 条款级）。** 学习项原文：「RFP 响应必须逐条回答，编号原样保留，每条标注支持 / 部分支持 / 不支持并给证据，不得用『后续规划』代替当前能力。」
本次材料里有条款 R-17 到 R-21。

| id | text | howToJudge | evidence | hard |
|---|---|---|---|---|
| c1 | R-17 到 R-21 五个编号在响应文档里逐个出现，编号与原文一致 | 打开响应文档，逐个搜索「R-17」到「R-21」，五个都在且未改写 | artifact | 硬 |
| c2 | 每个编号有独立响应段落，没有两条合并成一段回答 | 每个编号下方是自己的段落，不出现「R-17 到 R-21 均可支持」这类合并句 | artifact | 硬 |
| c3 | 每条标注「支持 / 部分支持 / 不支持」三者之一 | 每个编号段落第一句含这三个词之一 | artifact | 硬 |
| c4 | 每条「支持」或「部分支持」附证据或「待补证据」标记 | 段落内有证据句（配置说明、截图名、文档名）或字面「待补证据」 | artifact | 硬 |
| c5 | 全文不用「后续规划」「持续扩展」「未来支持」替代当前能力 | 全文搜索这三个词，命中处都不是在回答能力问题 | artifact | 硬 |
| c6 | R-20 写明当前已支持的是飞书、企业微信、钉钉中的哪一个，不泛称「主流 IM」 | R-20 段落点名具体 IM；出现「主流 IM」即不过 | artifact | 软：三选一来自本次条款原文，学习项没写 |

c1 到 c5 的 `sourceItemId` 填那条学习项；c6 来自本次条款材料，不填。

**反例（空话版，不能这样写）：**

- 「响应文档质量高」：没有判法，没有证据类型。
- 「符合客户 RFP 要求」：「要求」是什么没展开，换个人判不出同一个结论。
- 「逐条专业回答」：「专业」不可判；「逐条」可判但没写怎么判。
- 「审批流支持不少于 5 级」：这个数字来自 R-17 原文时可以写成硬标准；材料里没有这个数字，就只能标软并注明假设。

## 五、立目标

先看工作区有没有 `.gate/goal.json`。已有进行中的目标（`status` 为 `active`）就不再立；新发现的标准并入你手上的自检清单，收尾审计时一并核。
上一个目标已完成时，新目标直接覆盖它。

把目标写成 `.gate/goal.json`，字段如下：

- `status`：新立时写 `"active"`。
- `objective`：一段话写清**最终状态**：什么东西存在、在哪里、给谁、满足什么。写结果不写过程，写到别人拿着它能核。
  不写「做好标书」，写「工作区 `RFP响应.md` 里对 R-17 到 R-21 逐条给出状态、证据与风险」。
- `intent`：第二节那五行。
- `rubric`：`{ criteria: [...], precision: "precise" | "coarse" }`，criteria 按第四节写。
  **能用脚本判死的标准，给它挂一个 `validator`**（机器一眼能看出来做没做到的事：某些词不能出现、某些章节必须按顺序存在、
  某些编号必须齐、某个文件必须在某个目录、数字必须来自某张表）。可用类型见 `scripts/gate_check.py` 顶部注释。
  判不死的（语气、逻辑、是否「专业」）不挂 validator，留给审计和独立评估。
- `level`：`"L3"` 或 `"L4"`。L2 不立目标。
- `tokenBudget`：用户没要求就不填。

时机：意图和标准写完就立，在重活开始之前。立完用一句话告诉用户你会按哪几条验收；不贴 JSON，不讲内部机制。

示例：

```json
{
  "status": "active",
  "objective": "在工作区 RFP响应.md 中对客户 RFP 条款 R-17 至 R-21 逐条给出「支持/部分支持/不支持」状态、证据与风险说明，无法满足项显式列出，全文不用「后续规划」替代当前能力。",
  "intent": "交付物：RFP 逐条响应文档。消费者：销售交客户采购部评标。成功信号：五条各有独立状态与证据。非目标：不改产品能力、不写方案书。错一次的代价：对外交付，多承诺一条即合同风险。",
  "rubric": {
    "precision": "precise",
    "criteria": [
      { "id": "c1", "text": "R-17 到 R-21 五个编号逐个出现且与原文一致", "howToJudge": "逐个搜索五个编号，都在且未改写", "expectedEvidence": "artifact", "hard": true, "sourceItemId": "<学习项 ID>" },
      { "id": "c5", "text": "全文不用「后续规划」「持续扩展」「未来支持」替代当前能力", "howToJudge": "全文搜索三个词，命中处都不是在回答能力问题", "expectedEvidence": "artifact", "hard": true, "sourceItemId": "<学习项 ID>",
        "validator": { "type": "forbidden_terms", "file": "RFP响应.md", "terms": ["后续规划", "持续扩展", "未来支持"] } }
    ]
  },
  "level": "L4"
}
```

## 六、收尾礼仪

**完成审计（宣布完成之前）。** 把「已完成」当作未证明，对照当前真实状态逐条核：

- 每条标准，找能证明它的权威证据：文件里的原文、回复正文、工具调用记录。看当前状态，不靠记忆和「我刚才做过」。
- 证据不确定、间接、只是「和完成不矛盾」，一律算未完成，继续干。
- 核查范围要和标准范围一样宽：不能用一处抽查支撑「全文都对」。
- 不缩目标：不把成功重新定义成已经做完的那部分；不用更窄、更稳、更容易过的替代品换掉用户要的东西。
- 审计必须证明完成，而不是「没找到明显剩余工作」。

**「全都要」型标准怎么核。** 标准里带「每条」「每节」「全文」「所有」这类范围词时，它管的是一组对象，不是一处。
先把对象逐个列成清单——哪几条事实、哪几个小节、哪几个文件、哪几个编号——清单列出来才知道这条标准要核几次；
再逐个回到产物里核，`evidence` 里每个对象各给一段原文引用。清单上有一个对象没有对应的引用，这条标准就还没核完。
看了一处没问题就写「已全部满足」，是把一次抽样当成了一次普查。

**确定性核对。** 运行 `python3 <本 skill 目录>/scripts/gate_check.py .gate/goal.json --root .`。它逐条执行挂了 `validator` 的标准，
把结果写进 `.gate/check.json` 并打印每条的过 / 不过与缺口。任何一条硬标准不过，就回去改产物，改完再跑，直到全部通过。
它说的话只是提醒，不是裁决——但硬标准不过而你要交付，必须能向用户说清为什么。

**脚本判死的只覆盖挂了 `validator` 的那几条。** 确定性校验器只跑 rubric 里挂了 `validator` 的标准，其余的它一条也没看。
所以 `gate_check.py` 打印的「有脚本校验的标准：0/N 条」是说「脚本这次没判死任何一条」，不是「这份 rubric 通过了」。
没挂 `validator` 的语义标准，每一条都要你自己从产物里引到原文；引不到就还是未完成。

**例（标准 c2「每条事实注明来源文件」，产物里有 5 条事实）。**

核过了的样子：先把五条事实列出来（第 1 段的部署量、第 2 段的时间线、第 3 段的两个客户名、第 4 段的价格），
逐条回产物找那句来源标注，`evidence` 写「c2：事实 1 引『……（见 deploy-log.md）』；事实 2 引『……（见 timeline.md）』；
事实 3 没有来源标注……」——五条各有一句引文或一句「没有」。第 3 条没有，这条就是未完成，回去补。

没核过的样子：看见第 1 条带着「（见 deploy-log.md）」，写下「c2：抽查第 1 条，有来源标注，通过」。
其余四条一次都没看，其中两条根本没写来源。这个结论是从一处推出来的，不是核出来的。

**独立评估（L4 必做，L3 建议做）。** 起一个独立的评估者：用你所在环境的子代理机制（Claude Code 用 `Agent` 工具，Codex 用 `spawn_agent`；
都没有就自己换一副「第一次看到这份产物」的眼睛重读）。
只给它三样东西：`.gate/goal.json`、产物文件路径、用户原始要求的原话；不给它你的过程、不给它你的结论。
要求它逐条给 `pass | fail | unverifiable`，每条附产物里的逐字引文、理由和修法，只读不改。把它的判决写进 `.gate/verdict.json`。

**标完成。** 全部核完，把 `.gate/goal.json` 的 `status` 改成 `complete`，并在同一文件的 `evidence` 字段逐条写：标准 id、你核了什么、在哪里（路径或原文引用）、结果。

**怎么对待建议。** 评估者的意见是建议，不是裁决，但默认要认：

- 有硬标准 fail 或 unverifiable：默认继续干。修完后重新审计、重新跑核对、重新评估。unverifiable 是「没核到」，不等于过。
- 只有能向用户说清「为什么这条建议不对，或为什么不值得再改」时才不采纳，而且要在回复里说出来。
- 软标准 fail：在回复里说明，可以交付。

**受阻。** 第一次遇到阻碍不许标 blocked。同一个阻碍连续三轮都没能推进，才把 `.gate/goal.json` 的 `status` 改成 `blocked` 并写 `reason`，
下一条消息说清卡在哪、需要用户或外部给什么。达到三轮之后，不许一边说受阻一边把目标留着。
难、慢、不确定、想先问一句，都不是 blocked。

**不许这样标完成：** 预算快用完、想停手、做完了一个更小的版本。这些都不是完成。

**交付时的说法。** 目标完成后，在回复里用几句话说明每条标准的结果；评估者有不同意见的，说你怎么处理的。
不解释门控、目标、评估这些内部机制。
